@model IEnumerable<QuizDTO>

@{
    ViewBag.Title = "Manage Quizzes";
}

<div class="container mt-4">
    <h1 class="text-center mb-5">Manage Quizzes by Course</h1>

    <div class="accordion" id="coursesAccordion">
        @foreach (var courseGroup in Model.GroupBy(q => q.CourseDTO))
        {
            var course = courseGroup.Key;
            var courseId = course.Id;

            <div class="accordion-item">
                <h2 class="accordion-header d-flex justify-content-between align-items-center" id="heading-@courseId">
                    <button class="accordion-button collapsed" type="button" data-bs-toggle="collapse" data-bs-target="#collapse-@courseId" aria-expanded="false" aria-controls="collapse-@courseId">
                        @course.Title (@course.Category) - @course.Level
                    </button>
                    <a href="@Url.Action("ManageQuizzes", new { courseId = courseId })" class="btn btn-outline-primary btn-sm ms-3">Manage Quizzes</a>
                </h2>
                <div id="collapse-@courseId" class="accordion-collapse collapse" aria-labelledby="heading-@courseId" data-bs-parent="#coursesAccordion">
                    <div class="accordion-body">
                        <!-- Display quizzes within this course -->
                        @if (!courseGroup.Any())
                        {
                            <p>No quizzes available for this course.</p>
                        }
                        else
                        {
                            <table class="table table-hover">
                                <thead>
                                    <tr>
                                        <th>Quiz Title</th>
                                        <th>Number of Questions</th>
                                        <th>Submissions</th>
                                        <th>Actions</th>
                                    </tr>
                                </thead>
                                <tbody>
                                    @foreach (var quiz in courseGroup)
                                    {
                                        <tr>
                                            <td>@quiz.Title</td>
                                            <td>@quiz.QuestionDTOs.Count</td>
                                            <td>@quiz.QuizSubmissionDTOs.Count</td>
                                            <td>
                                                <a href="@Url.Action("QuizDetails", new { quizId = quiz.Id })" class="btn btn-primary btn-sm">Details</a>
                                                <a href="@Url.Action("EditQuiz", new { quizId = quiz.Id })" class="btn btn-warning btn-sm">Edit</a>
                                                <a href="@Url.Action("QuizSubmissions", new { quizId = quiz.Id })" class="btn btn-info btn-sm">Submissions</a>
                                                <form asp-action="DeleteQuiz" method="post" class="d-inline">
                                                    <input type="hidden" name="quizId" value="@quiz.Id" />
                                                    <button type="submit" class="btn btn-danger btn-sm">Delete</button>
                                                </form>
                                            </td>
                                        </tr>
                                    }
                                </tbody>
                            </table>
                        }
                    </div>
                </div>
            </div>
        }
    </div>
</div>
